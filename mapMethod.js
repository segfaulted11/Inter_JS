// map() method accepts a callback function and applies that function to each element of of an array, and then it returns a new array. this is similar to forEach method. however, forEach doesnt return a new array.

let array = [1,2,3,4,5]

array.map((elements)=>{
    console.log(elements)
})